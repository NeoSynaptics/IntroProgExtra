#include <iostream> //innehåller cout
#include <thread>   //innehåller sleep_for
#include <chrono>   //innehåller milliseconds
#include <cmath>    //innehåller mattefunktioner
#include <iomanip>

/*
using namespace std;
using namespace this_thread;
using namespace chrono;

void bank ();

void task20(){

    bank();

}

struct allInfo {
    int accNum;
    string name;
    double balance;
};

void print(double amount,string name){
    if (amount<= 200){
        cout << name << "You have less than 200 dollars in acc";
    }
}

void printLarge (struct allInfo largeAmount )
void bank(){
   int amount =1;
   struct allInfo database[amount];


   for (int i=0; i<amount; i+=1){
       cout << "accNum, Name, Balance";
          cout << endl;

   cin >> database[i].accNum;
   cin >> database[i].name;
   cin >> database[i].balance;
   }

   for (int i=0; i<amount; i+=1){
   print (database[i].balance, database[i].name);
   }
   for (int i=0; i<amount; i+=1){
       if (database[i].balance>1000)
           database[i].balance+=100;

      print (database[i].name << database[i].balance) << "You have more than 1000 dollars therefor increment 100";
   }
}

*/
